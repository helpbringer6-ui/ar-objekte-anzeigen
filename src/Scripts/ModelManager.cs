using UnityEngine;

public class ModelManager : MonoBehaviour
{
    public GameObject[] furnitureModels;

    public void LoadModel(int index)
    {
        // Load the selected furniture model into the scene
    }

    public void AdjustModelSize(float newSize)
    {
        // Adjust the size of the placed model
    }
}
